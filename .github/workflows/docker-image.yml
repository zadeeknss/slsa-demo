name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:
    name: build and push to dockerhub
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker buildx build -t zadeeknss/nodeapp:$(date +%s) .
